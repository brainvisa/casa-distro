#! /usr/bin/env python

import os.path as osp
import sys

try:
    from casa_distro.command import main
except ImportError:
    sys.path.append(osp.join(osp.dirname(osp.dirname(osp.realpath(__file__))), 
                             'python'))
    from casa_distro.command import main
    
from casa_distro import admin_commands

main()